# MAS Stack Scaffold: Moodle 3.x (Legacy Exception)

This placeholder marks the bundled MAS stack preset for Moodle 3.x legacy
exception-track projects. Stack memory (constraints, security controls, and
migration expectations) is materialized from `src/specify_cli/mas.py` during
`specify init --stack moodle3`.
